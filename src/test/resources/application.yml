spring:
  profiles:
    active: test
  redis:
    port: 6370
  rsocket.server.port: 7000

logging.level.org.springframework.data.r2dbc: DEBUG

minio:
  bucket: moeawebframework
  accessKey: minioadmin
  secretKey: minioadmin
  endpoint: 'http://localhost:9000'